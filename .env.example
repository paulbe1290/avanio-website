# Make.com-Webhook für das Lead-Formular (Pflicht für /api/lead).
# In Vercel als Environment Variable hinterlegen, niemals committen.
MAKE_WEBHOOK_URL=

# Optional: finale Produktiv-Domain (Fallback: https://www.avanio.de)
NEXT_PUBLIC_SITE_URL=
